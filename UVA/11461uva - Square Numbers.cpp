#include<bits/stdc++.h>
#include<math.h>
using namespace std;
int main()
{
    int a,b,i,n,l;
    while(scanf("%d %d",&a,&b)==2)
    {
        if(a==0 || b==0)
            break;
        l=0;
        for(i=a;i<=b;i++)
        {
            if(n==)
            n=sqrt(i);
            l++;
        }
        printf("%d\n",l);
    }
}
